import { DoneTodoPipePipe } from './done-todo-pipe.pipe';

describe('DoneTodoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DoneTodoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
